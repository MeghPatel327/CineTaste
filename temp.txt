Note : Do only brainstorm, im using "movie" for both movie and series.

I want to make a site which have these things :

Common things: 
- User based system with one admin

User: 
- can see total watched , pending or total no. of movies, next 5 watch order movies/series in a list.
- use have to add movies manually (can add, remove, edit, read) from our custom form which have 4 field : Movie_or_series_name, link_to_watch_download (optional), rating (keep empty if pending), watch_order_rank (only if pending)
- Every use have to make a unique username and password and if possible have an email connected to it
- show which type of movie user likes/dislikes by searching for movie names online and usin old user's rating, make a graph of it.
- if there is no link_to_watch_download, I want to give some avaliable links to watch/download for free if possible (these sites will be called "pirate_sites" in our project and base urls for these sites will be added by the admin only)
- OPTIONAL : import/export .csv

Admin: 
- can see total no. of users registered and if possible total live users (online users)
- can see all usernames and emails of user
- can delete/block and user
- can see total no. of movies added, total no. of movies pending, total no. of movies watched per user
- can't change details of user's movie list
- can update pirate_sites list 

tools/ services I want to use : 
- vercel for hosting
- firebase/baserow for data storage (baserow preferred)




=====================================

- we will make out own Authentication system too, dont use firebase for that: 
-- we will use a custom auth system which have a username and password(encrypted) stored in the baserow table and we will use this to make a custom login system.
- is TMDB API free ?
- there should be multiple tables in the baserow like: users(username(pk), password, role(user/admin default user)), movies(username(pk), Movie_or_series_name(unique for Every user), link_to_watch_download, rating, watch_order_rank)
- I want a custom suggestion system for movies and series based on the user's rating and likings
- watch_order_rank will changed based on the GUI buttons (user will press up/down button to change the rank)
- there should be a page to see all the movies added by the user with filters, search and sort options

- Minimum or zero use of AI if possible.

For V1 forgot emai, we will add that in letter versions


=====================================

Great I liked almost all the things you told, remember those ideas for now. 
Also something from my side : 
- while user is adding movies, we will use TMDB to facht the movie details and show them in the form (for eg. Release year, genre, etc.). These fields are optional and will be field automatically wen user enters Movie_or_series_name only. And when user adds rating we will use this prefetched details to feed the suggestions system.
- one movie can have multiple genre, so our system should handle it too.
- this is full V1 and will be made by 100% vibe coding so you work is to understand the idea and give me prompts for IDE agents. 