# Agent Prompt Guide

One feature per prompt.

Always include:
Objective
Requirements
Acceptance Criteria
Constraints
Files affected
Out of scope

Never ask agent to build whole application in one prompt.
